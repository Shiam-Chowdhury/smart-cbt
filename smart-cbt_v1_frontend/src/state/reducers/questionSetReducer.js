const reducer = (state = [], action) => {
    switch(action.type){
        case "getQuestionSets":
            {
                let questionSets = [];
                return questionSets;
            }
    }
};

export default reducer;