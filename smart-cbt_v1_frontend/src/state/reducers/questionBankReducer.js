const reducer = (state = [], action) => {
    switch(action.type){
        case "getQuestions":
            {
                let questions = [];
                return questions;
            }
    }
};

export default reducer;